import rallax from 'rallax.js'

document.addEventListener('DOMContentLoaded', () => {
  const dead = rallax('.dead')
  const simple = rallax('.simple', {speed: 0.6})
  const parallax = rallax('.parallax', {speed: 0.4})
  const scrolling = rallax('.scrolling', {speed: 0.7})

  
})
